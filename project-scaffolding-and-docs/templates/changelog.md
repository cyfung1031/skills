<!--
TEMPLATE: CHANGELOG.md — what actually shipped, for users. Newest first, dated.
User-facing prose, not commit messages. Each entry should say what changed, why
it mattered, and the concrete symptom/capability it affected. Use anonymized or
synthetic cases in reusable templates; do not include private file paths,
customer names, project names, or proprietary measurements.
Format: Keep a Changelog + Semantic Versioning are good defaults.
Delete guidance comments.
-->

# Changelog

## <version> — <YYYY-MM-DD>

- **<What changed, in the user's terms>** (<category, e.g. bug fix / feature>).
  <Why it mattered — the symptom it removed or the capability it added. Anchor
  with a sanitized case such as `case-import-timeout` or `<workflow-name>`, not
  a private path or customer detail. Include measured impact if safe to share.>
- ...

## <previous version> — <YYYY-MM-DD>

- ...
